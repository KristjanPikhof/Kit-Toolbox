# shortcuts.conf - Directory shortcuts for 'goto' command
# Format: shortcut_name|full_path|description
#
# Fields (pipe-separated):
# 1. Shortcut name (used with: kit goto <name>)
# 2. Full path (can use ~ for home directory)
# 3. Description

# Development projects
claude|~/.claude/|Claude CLI directory
opencode|~/.config/opencode/|Opencode CLI directory
